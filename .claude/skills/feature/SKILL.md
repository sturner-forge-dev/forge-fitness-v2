# .claude/skills/feature/SKILL.md

## Feature Implementation

- Use shadcn/ui components for all UI
- Use TanStack Router <Link> for navigation
- Use TanStack Form useField hook (not render props)
- Do NOT use `as any` or biome suppressions
- Do NOT start the dev server
- Ask clarifying questions before exploring the codebase
- Run `npx tsc --noEmit --incremental` after changes
